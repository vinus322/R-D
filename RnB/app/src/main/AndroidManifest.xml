<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.ymdroid.rnb">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CLEAR_APP_CACHE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".event.Splash" />
        <activity android:name=".SignUp" />
        <activity android:name=".page.menu" />
        <activity android:name=".page.mypage" />
        <activity android:name=".page.mystory" />
        <activity android:name=".page.myrobot" />
        <activity android:name=".page.mymusic" />
        <activity android:name=".page.myemotion"></activity>

    </application>


</manifest>
